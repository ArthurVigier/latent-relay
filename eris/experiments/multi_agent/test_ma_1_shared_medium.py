"""
eris/experiments/multi_agent/test_ma_1_shared_medium.py
========================================================

Kill-gate MA-1 — SHARED_MEDIUM accuracy benefit.

Stub.  Run only after MA-0 passes (ISOLATED baseline confirmed).

Measures whether SHARED_MEDIUM mode improves accuracy over ISOLATED
on a held-out problem set.  Criterion: accuracy delta ≥ 3pp.
"""

raise NotImplementedError(
    "MA-1 is a stub.  Run test_ma_0_isolation_baseline.py first and confirm it passes."
)
