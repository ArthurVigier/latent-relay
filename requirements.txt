# Latent Relay Server — Phase 1
torch>=2.8.0          # <2.5.0 → PYSEC-2024-259 / <2.6.0 → PYSEC-2025-41 / <2.7.1 → GHSA-3749 / <2.8.0 → GHSA-887c
transformers>=4.53.0  # <4.52.1 → GHSA-37mw / <4.53.0 → GHSA-4w7r
accelerate>=0.25.0

# FastAPI server
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.4.0       # <2.4.0 → GHSA-mr82-8j83-vxmv

# MCP server
mcp>=1.23.0           # <1.9.4 → GHSA-3qhf / <1.10.0 → GHSA-j975 / <1.23.0 → GHSA-9h52

# Test client
requests>=2.32.4      # <2.32.0 → GHSA-9wx4 / <2.32.4 → GHSA-9hjg

# Probe loading (required for .pkl/.joblib SAE probes — no pickle.load)
joblib>=1.3.0

# ERIS v5 — required
numpy>=1.24.0
pyyaml>=6.0.0
scikit-learn>=1.5.0       # <1.5.0 → PYSEC-2024-110
httpx>=0.27.0

# ERIS v5 — Claude client (eris_client.py / ClaudeZombieBridge)
anthropic>=0.40.0

# Evaluation (eval/eval_phase1.py)
datasets>=2.20.0
scipy>=1.10.0
tqdm>=4.0.0
duckduckgo-search>=6.0.0   # frontier/webdialogue web search (no API key needed)

# Testing
pytest>=8.0.0

# ERIS v5 — optional lazy-loaded analyzers
# sae-lens>=5.0.0       # SAEs pré-entraînées (pip install sae-lens)
# a-hat-optimizer       # Sonde Â-hat (install depuis le repo Arthur)
