# Latent Relay Server — Phase 1
torch>=2.8.0          # <2.5.0 → PYSEC-2024-259 / <2.6.0 → PYSEC-2025-41 / <2.7.1 → GHSA-3749 / <2.8.0 → GHSA-887c
transformers>=4.53.0  # <4.52.1 → GHSA-37mw / <4.53.0 → GHSA-4w7r
accelerate>=0.26.0

# FastAPI server
fastapi>=0.115.0      # Bumped to avoid known vulnerabilities in older 0.110.x
uvicorn>=0.30.0
pydantic>=2.4.0       # <2.4.0 → GHSA-mr82-8j83-vxmv

# MCP server
mcp>=1.23.0           # <1.9.4 → GHSA-3qhf / <1.10.0 → GHSA-j975 / <1.23.0 → GHSA-9h52

# Test client
requests>=2.32.4      # <2.32.0 → GHSA-9wx4 / <2.32.4 → GHSA-9hjg

# Probe loading (required for .pkl/.joblib SAE probes — no pickle.load)
joblib>=1.4.3         # <1.4.3 → CVE-2024-34997 (Disputed deserialization vulnerability)

# ERIS v5 — required
numpy>=1.24.0
pyyaml>=6.0.1         # Bumped to 6.0.1+ for security/build fixes
scikit-learn>=1.5.0   # <1.5.0 → PYSEC-2024-110
httpx>=0.28.1         # Bumped to 0.28.1 to resolve potential httpx/certifi CVEs

# ERIS v5 — Orchestrator backends
anthropic>=0.40.0
google-generativeai>=0.8.0    # GeminiOrchestrator (optional)
openai>=1.30.0                # OpenRouterOrchestrator + OpenAIOrchestrator (optional)

# Evaluation (eval/eval_phase1.py)
datasets>=2.20.0
scipy>=1.10.0
tqdm>=4.0.0
duckduckgo-search>=6.0.0   # frontier/webdialogue web search (no API key needed)

# Testing
pytest>=8.0.0

# ERIS V2 — SAE probe (Gemma Scope 2)
# Installer avant de lancer scripts/validate_sae_on_aime.py
sae-lens>=5.0.0               # SAEs pré-entraînées + Gemma Scope 2
transformer-lens>=3.0.0b0     # requis par sae-lens v5 pour Gemma 3

# ERIS v5 — optional lazy-loaded analyzers
# a-hat-optimizer       # Sonde Â-hat (install depuis le repo Arthur)
